/**
 * React Native CLI configuration.
 *
 * No autolinking exclusions needed — build issues are handled by
 * Expo config plugins (fix-onnx-gradle.js, exclude-midi-android.js).
 */
module.exports = {};
